export default function Loading() {
  return (
    <main id="main-content" className="loading-state" aria-live="polite" aria-busy="true">
      <span className="loading-mark">R</span>
      <p>Opening the operations room...</p>
    </main>
  );
}
